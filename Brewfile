# Brewfile — macOS packages
# Add/remove a line, then: brew bundle
# ─────────────────────────────────────────────────────────────────────────────

# ── bootstrap essentials ──────────────────────────────────────────────────────
brew "git"
brew "stow"
brew "curl"

# ── shell utilities (sourced unconditionally in config.fish) ──────────────────
brew "direnv"
brew "fzf"
brew "ripgrep"
brew "bat"
brew "zoxide"

# ── shell + prompt ────────────────────────────────────────────────────────────
brew "fish"
brew "starship"

# ── editor ────────────────────────────────────────────────────────────────────
brew "neovim"

# ── terminal multiplexer ──────────────────────────────────────────────────────
brew "tmux"

tap "jrmoulton/tap"
brew "jrmoulton/tap/tmux-sessionizer"
